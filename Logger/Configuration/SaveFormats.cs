namespace CodeDead.Logger.Configuration
{
    /// <summary>
    /// Enum that contains the various formats in which a LoggerRoot object can be stored or loaded
    /// </summary>
    public enum SaveFormats
    {
        Json,
        Xml
    }
}
